import ScrollFillText from './ScrollFillText'

export default function ScrollFillTextStory() {
  return (
    <ScrollFillText>
      Two men look out through the same bars; One sees the mud, and one the stars.
    </ScrollFillText>
  )
}
